# Bilinear form

二元函数（接受两个同一 vector space 的向量），对于每一个参数都线性

$$\begin{aligned}
    & B(\bm x,\bm  y) \\
    =& B\left( \sum_i x_i \bm e_i, \sum_j y_j \bm e_j \right) \\
    =& \sum_i \sum_j B(\bm e_i, \bm e_j) x_i y_j
\end{aligned}$$

$$B(\bm x, \bm y) = \sum_{i,j} A_{i,j} x_i y_j$$

记

$$B_A(\bm x, \bm y) = \sum_{i,j} A_{i,j} x_i y_j$$

构造成矩阵与向量的乘法：（符号滥用中）

$$B(\bm x, \bm y) = \bm x^T A \bm y$$

虽然这个符号有点意义不明，但是由于它实在是过于方便，因此我们会一直使用。

## 简单性质

$$B(\bm x,\bm y) = B(\bm y,\bm x) \iff A = A^T$$

$$A_{i,j} = B(\bm e_i, \bm e_j) = B(\bm e_j, \bm e_i) = A_{j,i}$$

---

$$B(\bm x,\bm y) = -B(\bm y,\bm x) \iff A = - A^T$$

$$A_{i,j} = B(\bm e_i, \bm e_j) = - B(\bm e_j, \bm e_i) = - A_{j,i}$$

$$B(\bm x,\bm x) = 0$$

---

$$A = S + K$$

$$A = \frac {A + A^T} 2 + \frac {A - A^T} 2$$

Symmetric bilinear form

## Quadratic form

二次型：

$$Q(\bm x) = B(\bm x,\bm x)$$

$$B(\bm x,\bm x) = \bm x^T (S + K) \bm x = \bm x^T S \bm x + \bm x^T K \bm x = \bm x^T S \bm x$$

只讨论对称的二次型

---

二次型与对称双线性型的双射（极化恒等式）：

$$\boxed{
    B(\bm x, \bm y) = \frac {Q(\bm x + \bm y) - Q(\bm x) - Q(\bm y)} 2
}$$

# 多元函数 Taylor 展开 & Hessian 矩阵

要求 $f(\bm x_0 + \bm h)$ 的展开。设辅助函数 $g$

$$g(t) = f(\bm x_0 + t \bm h) \implies f(\bm x_0 + \bm h) = g(1) = \sum_{n=0}^\infty \frac {g^{(n)}(0)} {n!}$$

$$\begin{cases}
    g'(t) = \sum_i \frac {\partial} {\partial x_i} f(\bm x_0 + t \bm h) \times h_i \\
    g''(t) = \sum_{i,j} \frac {\partial^2} {\partial x_i \partial x_j} f(\bm x_0 + t \bm h) \times h_i \times h_j \\
\end{cases}$$

$$\begin{cases}
    g'(0) = \sum_i \frac {\partial} {\partial x_i} f(\bm x_0) \times h_i \\
    g''(0) = \sum_{i,j} \frac {\partial^2} {\partial x_i \partial x_j} f(\bm x_0) \times h_i \times h_j \\
\end{cases}$$

$g''(0)$ 是一个关于 $\bm h$ 的二次型。系数矩阵记作 $H(\bm x_0)$。

$$g''(0) = \bm h^T H(\bm x_0) \bm h$$

$$H(\bm x_0)_{i,j} = \frac {\partial^2} {\partial x_i \partial x_j} f(x_0)$$

偏导可以交换，因此 $H(\bm x_0)$ 天然是对称矩阵。

$$\boxed{
    f(x_0 + h) = f(x_0) + (\nabla f(x_0))^T h + \frac 1 2 h^T H(x_0) h + o(\lVert h \rVert^2)
}$$

## 极值点

$$\nabla f(\bm x_0) = 0$$

$$f(\bm x_0 + \bm h) - f(\bm x_0) = \frac 1 2 \bm h^T H(\bm x_0) \bm h + o(\lVert \bm h \rVert^2)$$

若 $H$ 满足

$$\bm h^T H \bm h > 0, \quad \forall \bm h \ne 0$$

则是局部的极小值。

对称矩阵 $A$

$$\bm x^T A \bm x > 0, \quad \forall \bm x \ne 0$$

称为正定矩阵

# 内积 & 度量

当 $A$ 是正定矩阵时，对应的双线性型被称为**内积**，记作 $\langle \bm x, \bm y \rangle$。$A$ 被称为**度量**。内积为 $0$ 的向量**正交**。

**内积诱导范数**：

$$\lVert \bm x \rVert = \sqrt{\langle \bm x, \bm x \rangle}$$

## 内积的基本性质

$$\langle \bm x, \bm y \rangle = \langle \bm y, \bm x \rangle$$

$$\langle A \bm x, \bm y \rangle = \langle \bm x, A^T \bm y \rangle$$


## 勾股定理

$$\langle \bm x, \bm y \rangle = 0 \implies \lVert \bm x \rVert^2 + \lVert \bm y \rVert^2 = \lVert \bm x + \bm y \rVert^2$$

## 投影

$$\begin{cases}
    \bm x = \bm x_\parallel + \bm x_\perp \\
    \bm x_\parallel = \lambda \bm y \\
    \langle \bm x_\perp, \bm y \rangle = 0
\end{cases}$$

解方程组

$$\begin{aligned}
    \langle \bm x - \lambda \bm y, \bm y \rangle &= 0 \\
    \langle \bm x, \bm y \rangle - \lambda \langle \bm y, \bm y \rangle &= 0 \\
    \lambda &= \frac {\langle \bm x, \bm y \rangle} {\lVert \bm y \rVert^2}
\end{aligned}$$

$$\bm x_\parallel = \frac {\langle \bm x, \bm y \rangle} {\lVert \bm y \rVert^2} \bm y$$

$$\boxed{
    \bm x_\parallel = \frac {\langle \bm x, \bm y \rangle} {\lVert \bm y \rVert} \hat y
}$$

若 $\bm y$ 是一个单位向量（一般记作 $\bm u$）：

$$\bm x_\parallel = \langle \bm x, \bm u \rangle \hat {\bm u}$$

## Cauchy-Schwarz 不等式

$$\boxed{
    \lvert \langle \bm x, \bm y \rangle \rvert \le \lVert \bm x \rVert \lVert \bm y \rVert
}$$

证明：把 $\bm x$ 正交分解到 $\bm y$ 的方向和与 $\bm y$ 正交的方向。

$$\begin{aligned}
    \bm x &= \frac {\langle \bm x, \bm y \rangle} {\lVert \bm y \rVert} \hat {\bm y} + \bm x_\perp \\
    \lVert \bm x \rVert^2 &= \frac {\lvert \langle \bm x, \bm y \rangle \rvert^2} {\lVert \bm y \rVert^2} + \lVert \bm x_\perp \rVert^2 \\
    \lVert \bm x \rVert^2 & \ge \frac {\lvert \langle \bm x, \bm y \rangle \rvert^2} {\lVert \bm y \rVert^2} \\
    \lVert \bm x \rVert \lVert \bm y \rVert & \ge \lvert \langle \bm x, \bm y \rangle \rvert
\end{aligned}$$

当 $\bm y = \bm 0$ 时原结论是平凡的，因此不担心 $\lVert \bm y \rVert = 0$ 在分母上的情况。

取等条件是 $\bm x_\perp = \bm 0$，即 $\bm x$ 和 $\bm y$ 线性相关。

## Gram–Schmidt process

TODO:

任意内积空间存在正交基

## 正交矩阵

保内积不变的变换（显然也保范数不变）

$$\langle \bm x, \bm y \rangle = \langle Q \bm x, Q \bm y \rangle$$

$$Q^T Q = I$$

$$(\det Q)^2 = 1 \implies \det Q = \pm 1$$

旋转，可能翻转。构成 Orthogonal Group $O(n)$

若只看 $\det Q = 1$，强制不许翻转。构成 Special Orthogonal Group $SO(n)$

# 正交对角化

TODO:

对于实对称矩阵 $A$

$$\langle A \bm x, \bm y \rangle = \langle \bm x, A \bm y \rangle$$

$A$ 的所有特征值均为实数

取一个特征向量，取它张成空间的正交补

正交补对与特征向量点积封闭

$$A = Q^T \Lambda Q$$

---

任意度量下的内积都相当于一个以对角阵为度量的内积

$$\langle \bm x, \bm y \rangle_A \equiv \langle \bm x, \bm y \rangle_\Lambda$$

## 判定正定矩阵的一个充要条件

该对称矩阵的所有特征值均为正。

TODO: 这个简单

## 正定矩阵的二次型

正定矩阵二次型的等值面是椭球

$$Q(x) = \sum_{k=1}^n \lambda_k y_k^2 = \sum_{k=1}^n \frac {y_k^2} {(1 / \sqrt{\lambda_k})^2}$$

极值

与单位球有公共点的最大椭球，一定是在最短轴顶点相切

$$\min \frac 1 {\sqrt{\lambda_k}} \implies \max \lambda_k$$

同理，最小值对应最小的特征值

# 某种结构

## 协方差张量

先去中心化。$\bm x \mapsto \bm x - \bar {\bm x}$

$$\operatorname{Var}(\langle \bm x, \bm u \rangle) = \frac 1 n \sum_k \langle \bm x_k, \bm u \rangle^2$$

这是一个二次型。不妨将中间的矩阵记作 $\Sigma$。

$$\operatorname{Var}(\langle \bm x, \bm u \rangle) = \bm u^T \Sigma \bm u$$

根据极化恒等式（或直接凭直觉将一个 $\bm u$ 替换为 $\bm v$），得到其对应的双线性型为

$$B(\bm u,\bm v) = \frac 1 n \sum_k \langle \bm x_k, \bm u \rangle \langle \bm x_k, \bm v \rangle$$

代入基向量 $\bm e_i, \bm e_j$ 可以得到 $\Sigma$ 的元素：

$$\begin{aligned}
    \Sigma_{i,j} = B(\bm e_i, \bm e_j) &= \frac 1 n \sum_k \langle \bm x_k, \bm e_i \rangle \langle \bm x_k, \bm e_j \rangle \\
    &= \frac 1 n \sum_k x_{k,i} x_{k,j} \\
    &= \operatorname{Cov}(X_i, X_j) \\
\end{aligned}$$

$$\Sigma = \Sigma^T$$

### 主成分分析 PCA

最大化

$$\Sigma \bm u = \lambda_{\max} \bm u$$

## 转动惯量张量

协方差——投影长度的平方；转动惯量——投影距离的平方。加起来是定值，所以是同一个数学结构。

# 各向同性

TODO: 现实世界！

各向同性的度量，即在任意正交矩阵的线性变换下都不变的度量

$$Q^T A Q = A$$

$$A = c I$$

## 点积

令 $c = 1$。

$$\bm x \cdot \bm y := \bm x^T I \bm y = \sum_i x_i y_i$$

勾股定理：

$$\lVert \bm x \rVert = \sqrt{\bm x \cdot \bm x} = \sqrt{\sum_i x_i^2}$$

# 三角函数

TODO:

# Hermite 内积

回顾范数：

$$\lVert \bm x \rVert^2 = \langle \bm x, \bm x \rangle$$

如果双线性的系数允许有复数？

$$\lVert \mathrm i \bm x \rVert^2 = \langle \mathrm i \bm x, \mathrm i \bm x \rangle = \mathrm i^2 \langle \bm x, \bm x \rangle = - \langle \bm x, \bm x \rangle = - \lVert \bm x \rVert^2$$

完啦！希望保证范数是（非负的）实数！

修改：

$$\langle a \bm x, b \bm y \rangle = a \bar b \langle \bm x, \bm y \rangle$$

共轭对称性？

$$\lVert \bm x + \lambda \bm y \rVert^2 = \lVert \bm x \rVert^2 + \bar \lambda \langle \bm x, \bm y \rangle + \lambda \langle \bm y, \bm x \rangle + \lvert \lambda \rvert^2 \lVert \bm y \rVert^2$$

选 $\lambda = 1, i$

最终

$$\langle \bm x, \bm y \rangle = \overline{\langle \bm y, \bm x \rangle}$$