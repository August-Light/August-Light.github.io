# Poisson distribution

> 有一个随机变量 $X \sim \mathcal B(n,p)$，可知 $\mathbb E[X] = np$。现在我们让 $\lambda = np$ 固定，但是 $n \to \infty$（与此同时 $p = \frac \lambda n \to 0$）。问此时 $X$ 的概率分布。
>
> 这样的概率分布记为：
>
> $$X \sim \text{Poisson}(\lambda)$$

$$P(X = k) = \binom n k p^k (1-p)^{n-k}$$

$$\begin{aligned}
    & P(X = k) \\
    =& \lim_{n \to \infty} \binom n k \left( \frac \lambda n \right)^k \left( 1 - \frac \lambda n \right)^{n-k} \\
    =& \frac {\lambda^k} {k!} \lim_{n \to \infty} \frac {n^{\underline k}} {n^k} \left( 1 - \frac \lambda n \right)^{-k} \left( 1 - \frac \lambda n \right)^n \\
    =& \frac {\lambda^k e^{-\lambda}} {k!}
\end{aligned}$$

## 期望和方差相等

对于 $X \sim \mathcal B(n,p)$：

$$\mathbb E[X] = np$$

$$\operatorname{Var}(X) = np(1-p)$$

当 $np = \lambda$ 且 $p \to 0$ 时 $\operatorname{Var}(X) \to \mathbb E[X]$，即 Poisson distribution 的期望和方差相等。

## 可加性

$$\begin{cases}
    X_1 \sim \text{Poisson}(\lambda_1) \\
    X_2 \sim \text{Poisson}(\lambda_2) \\
\end{cases} \implies X_1 + X_2 \sim \text{Poisson}(\lambda_1 + \lambda_2)$$

这可能需要对 Poisson 分布新的理解。

# PGF

$$G_X(s) = \mathbb E[s^X] = \sum_{k=0}^\infty P(X = k) s^k$$

## 性质

总概率为 $1$：

$$G_X(1) = 1$$

提取系数：

$$P(X = k) = \frac {G_X^{(k)}(0)} {k!}$$

矩：

$$\begin{cases}
    \mathbb E[X] = G_X'(1) \\
    \mathbb E[X (X-1)] = G_X''(1) \\
\end{cases}$$

$$\begin{aligned}
    \operatorname{Var}(X) &= \mathbb E[X^2] - \mathbb E[X]^2 \\
    &= G_X''(1) + G_X'(1) - G_X'(1)^2
\end{aligned}$$

独立变量 $X,Y$ 的卷积：

$$G_{X+Y}(s) = G_X(s) G_Y(s)$$

## 常见的 PGF

$X \sim \operatorname{Binomial}(n, p)$:

$$\begin{aligned}
    G_X(s) &= \sum_{k=0}^n \binom n k p^k (1-p)^{n-k} s^k \\
    &= (1 - p + ps)^n
\end{aligned}$$

$X \sim \operatorname{Poisson}(\lambda)$:

$$\begin{aligned}
    G_X(s) &= e^{- \lambda} \sum_{k=0}^\infty \frac {\lambda^k} {k!} s^k \\
    &= e^{- \lambda} e^{\lambda s} \\
    &= e^{\lambda (s-1)} \\
\end{aligned}$$