# CSP-S 2025

## [P14362 [CSP-S 2025] 道路修复](https://www.luogu.com.cn/problem/P14362) (Normal)

> TODO:

部分分一眼能看出这是个最小生成树的题。

注意到 $k$ 很小，考虑指数级 $O(2^k)$ 的东西。直接枚举子集求最小生成树。

时间复杂度 $O(2^k (kn + m) \log (kn + m))$，能拿不少分。

要多次求最小生成树，显然 Kruskal 是优的。我们预先排序然后改归并，这样去掉一个 $\log$。时间复杂度 $O(m \log m + kn \log n + 2^k (kn + m) \alpha(kn + m))$，能拿 80pts。

显然我们卡在 $m$ 上了，有没有办法把 $m$ 从 $2^k$ 这里弄走？

注意到一个最小生成树的性质：**对于一条不出现在最小生成树上的边，再多加边它也不会出现在最小生成树上**。

因此我们只要保留出现在原图最小生成树上的 $n-1$ 条边，而不是全部的 $m$ 条边就行了。

时间复杂度 $O(m \log m + k n \log n + 2^k kn \alpha(kn))$，可以满分。

[yuanruiqi 的题解](https://www.luogu.com.cn/article/zpde9js3)声称，根据子集的性质，稍做优化可以把 $2^k k$ 优化到 $2^k$，但是没有学会。TODO:

## [P14363 [CSP-S 2025] 谐音替换](https://www.luogu.com.cn/problem/P14363)

> 给定 $n$ 个字符串二元组，第 $i$ ($1 \leq i \leq n$) 个字符串二元组为 $(s_{i,1}, s_{i,2})$，满足 $|s_{i,1}| = |s_{i,2}|$，其中 $|s|$ 表示字符串 $s$ 的长度。
>
> 对于字符串 $s$，定义 $s$ 的**替换**如下：对于 $s$ 的某个子串 $y$，若存在 $1 \leq i \leq n$ 满足 $y = s_{i,1}$，则将 $y$ 替换为 $y' = s_{i,2}$。
>
> 小 W 提出了 $q$ 个问题，第 $j$ ($1 \leq j \leq q$) 个问题会给定两个**不同**的字符串 $t_{j,1}, t_{j,2}$，她想知道有多少种字符串 $t_{j,1}$ 的替换能够得到字符串 $t_{j,2}$。两种 $s$ 的替换不同当且仅当**子串 $y$ 的位置不同或用于替换的二元组 $(s_{i,1}, s_{i,2})$ 不同**，即 $x, z$ 不同或 $i$ 不同。你需要回答小 W 提出的所有问题。
>
> 令 $L_1 = \sum \lvert s \rvert, L_2 = \sum \lvert t \rvert$。
>
> $n,q \le 2 \times 10^5$，$L_1, L_2 \le 5 \times 10^6$，$\lvert \Sigma \rvert = 26$。

首先判掉 $\lvert t_{i,1} \rvert \ne \lvert t_{i,2} \rvert$ 的情况。😭

根据特殊性质，不难想到把 LCP 和 LCS 摘掉之后再考虑。

具体来说，对于 $s_{i,1}$ 和 $s_{i,2}$，我们拆成 $s_{LCP} + s'_1 + s_{LCS}$ 和 $s_{LCP} + s'_2 + s_{LCS}$。对于 $t_{i,1}, t_{i,2}$ 也一样。

接下来我们就只考虑每一对 $s', t'$ 之间的贡献，注意到这个贡献只能是 $0/1$。

$s'$ 能去把 $t'$ 替换掉的充要条件是：
- $s'_1 = t'_1$
- $s'_2 = t'_2$
- $\text{LCS}(s_{LCP}, t_{LCP}) = s_{LCP}$
- $\text{LCP}(s_{LCS}, t_{LCS}) = s_{LCS}$

能不能把这四个条件打包起来？

构造字符串

$$s_i = s_{LCP} + \# + s'_1 + \# + s'_2 + \# + s_{LCS}$$

$$t_i = t_{LCP} + \# + t'_1 + \# + t'_2 + \# + t_{LCS}$$

充要条件则可以直接写为 $s_i$ 是 $t_i$ 的子串。

此时变为多模字符串匹配问题。

TODO: 利用广义 SAM 在 $O(L_1 + L_2)$ 时间内解决，或是利用 AC 自动机在 $O(\lvert \Sigma \rvert L_1 + L_2)$ 时间内解决。

## [P14364 [CSP-S 2025] 员工招聘](https://www.luogu.com.cn/problem/P14364)✅️ (Hard)

> 有 $n$ 个人，每人有耐心值 $c_i$。你要决定一个面试顺序 $p$（$1 \sim n$ 的排列），每天面试一人 $p_i$，面试题难度 $s_i \in \{0,1\}$ 已知。若当天题难度为 $1$ 则录用，否则拒绝。如果到某人面试时，之前被拒绝或放弃的人数 $\ge c_i$，则此人放弃。问有多少个排列 $p$ 使得最终录用人数 $\ge m$。
>
> $m \le n \le 500$，时限 2s。

### Sol 1：[luanyanjia 的题解](https://www.luogu.com.cn/article/jphoz1vz)

以下用 $x += y$ 表示 $x \gets x + y$。

令 $a$ 为 $c$ 的值域数组，设 $b$ 是 $a$ 的前缀和数组。

设 $f_{i,j,k}$ 为

- 前 $i$ 天；
- 有 $j$ 人未录取；
- 有 $k$ 个人 $x$ 满足 $c_x > j$，**不考虑这些人顺序**；

的方案数。最终答案为 $\sum_{j=0}^{n-m} f_{n,j,n-b_j} \times (n - b_j)!$。

转移时，枚举 $t \in [0,k]$ 表示这 $k$ 个人中有几个人 $x$ 满足 $c_x = j+1$。这些人会产生的贡献是 $res = \binom {a_{j+1}} t k^{\underline{t}}$。

- Case 1：$s_{i+1} = 1$
  - 那就得看第 $i+1$ 天的这个人 $p_{i+1}$ 的 $c$ 是不是 $\le j$ 了。
  - 对于 $c_{p_{i+1}} \le j$ 这一类，面试失败，转移到 $j+1$ 的状态。
    - 我们要从剩下的 $\le j$ 的人里选一个。
    - 我们用掉了 $i$ 人，这 $i$ 人中有 $k$ 个是 $>j$ 的，因此剩下 $i-k$ 个是 $\le j$ 的；
    - 从 $b_j$ 个人中去掉它们，因此从 $b_j - (i-k) = b_j + k - i$ 个 $\le j$ 的人中选取一人。
    - $\boxed{f_{i+1,j+1,k-t} += res \times (b_j + k - i) \times f_{i,j,k}}$，**当括号里的项 $\ge 0$ 才转移**。
  - 对于 $c_{p_{i+1}} > j$ 这一类，面试成功，直接转不需要枚举 $t$，细算留给后面：
    - $\boxed{f_{i+1,j,k+1} += f_{i,j,k}}$。

- Case 2：$s_{i+1} = 0$，必面试失败，转移到 $j+1$ 的状态。
  - 因此我们要讨论 $c_{p_{i+1}}$ 和 $j+1$ 的关系，而不是和 $j$。
    - 对于 $c_{p_{i+1}} \le j+1$ 这一类：
      - 和上面同理，我们要从剩下的 $\le j+1$ 的人里选一个。
      - 我们用掉了 $i$ 人，这其中有 $k - t$ 个是 $> j+1$ 的，因此剩下 $i - (k-t)$ 个是 $\le j+1$ 的。
      - 从 $b_{j+1}$ 个人中去掉他们，因此从 $b_{j+1} - (i - (k - t)) = b_{j+1} + k - i - t$ 个人中选取一人。
      - $\boxed{f_{i+1,j+1,k-t} += res \times (b_{j+1} + k - i - t) \times f_{i,j,k}}$，**当括号里的项 $\ge 0$ 才转移**。
    - 对于 $c_{p_{i+1}} > j+1$ 这一类：
      - $\boxed{f_{i+1,j+1,k-t+1} += res \times f_{i,j,k}}$

这是一个 3D-1D DP，时间复杂度 $O(n^4)$。

但是事实上精细实现就是 $\Theta(n^3)$：把 $t$ 的枚举范围从 $[0,k]$ 缩到 $[0,\min(a_{j+1},k)]$ 即可，因为 $\sum a_j$ 是 $\Theta(n)$ 的。

与此同时，注意到 $i$ 的状态总是转移到 $i+1$ 的状态，因此可以滚动数组压掉一维，空间复杂度 $O(n^2)$。

### Sol 2

https://www.luogu.com.cn/article/wex2isk9

https://www.luogu.com.cn/article/33brin1o

TODO:

## 附：[P7880 [Ynoi2006] rldcot](https://www.luogu.com.cn/problem/P7880)

# 17 套 (Easy)

## [P3898 [湖南集训] 大新闻](https://www.luogu.com.cn/problem/P3898) (Easy)

略。

## [P14260 期待（counting）](https://www.luogu.com.cn/problem/P14260) (Easy)

大分讨，略。

## [P9790 [ROIR 2020] 海报 (Day 2)](https://www.luogu.com.cn/problem/P9790) (Easy)

非常简单的环上动态 DP，略。

# 18 套（11.5）

## [P13349 「ZYZ 2025」自然数序列](https://www.luogu.com.cn/problem/P13349) (Normal)

> （原题面）
>
> 给定长度为 $n$ 的**正整数**序列 $a$，有 $q$ 次询问。对于每次询问，给出 $l,r,k$ 和 $k$ 个限制条件，求有多少个长度为 $n$ 的**自然数**序列 $b$ 满足 $l\le\sum\limits_{i=1}^na_ib_i\le r$ 且满足这 $k$ 个限制条件，对 $998244353$ 取模。
>
> 对于每个限制条件，给出 $x,y$，要求 $b_x=y$。
>
> $n \le 5 \times 10^3$，值域 $5 \times 10^3$，$q \le 5 \times 10^4$，$0 \le k \le 8$。
>
> - 对于 25pts，每次询问 $k=1$ 且 $l=r$。
> - 对于另外 25pts，每次询问 $l=r$。

完全背包的题，要么是巧妙 DP 要么是生成函数。事实上两个都可以做。

TODO: 容斥



## [P12976 受力分析 Force](https://www.luogu.com.cn/problem/P12976)✅️ (Normal)

> 有两个**自然数**序列 $x_{[1,n]}, y_{[1,n]}$，但是你不知道值。
>
> 给定两个 $n \times n$ 矩阵 $L,R$。有 $n^2$ 个限制形如：
>
> $$L_{i,j} \le x_i + y_j \le R_{i,j}$$
>
> 求出字典序最小的 $x$ 和 $y$（先比较 $x$ 字典序，再比较 $y$ 字典序）。
>
> $n \le 500$。

一堆耦合的双变量不等式，想到差分约束。SPFA 复杂度 $O(nm) = O(n^3)$，那应该就是对了。

构造变量 $z_j = -y_j$，那么 $x_i + y_j$ 可以被写为 $x_i - z_j$。

由于我们要求最小解，所以我们要把每个不等式都写为 $\ge$ 的形式：

$$\begin{cases}
    x_i - z_j \ge L_{i,j} \\
    z_j - x_i \ge - R_{i,j} \\
\end{cases}$$

除此以外不要忘记 $x,y$ 非负的限制：

$$\begin{cases}
    0 - z_j \ge 0 \\
    x_i - 0 \ge 0 \\
\end{cases}$$

我们建一个 $dis$ 是 $0$ 的新点即可刻画这一限制。

由于边权有正有负，跑 SPFA 最长路求解。

但是，我们虽然成功最小化了 $x$ 的字典序，但是并没有对 $y$ 最小化，反而是最大化了。但我们又注意到，当 $x$ 确定时 $y$ 的最小值非常容易确定，那就做完了。

时间复杂度 $O(n^3)$。

## [P10716 【MX-X1-T4】「KDOI-05」简单的字符串问题](https://www.luogu.com.cn/problem/P10716) (Hard)



## [P13241 「2.48sOI R1」格律树](https://www.luogu.com.cn/problem/P13241) (Easy)

一眼看出虚树，特征过于明显了。再一眼看出用矩阵描述 DP，做完了。

# 19 套

## [P6241 [eJOI 2019] 塔](https://www.luogu.com.cn/problem/P6241) (Ad-hoc)

略。

## [P6217 简单数论题](https://www.luogu.com.cn/problem/P6217) (Easy)

略。

## [P11882 [RMI 2024] 彩虹糖 / Skittlez](https://www.luogu.com.cn/problem/P11882) (Hard)

>

TODO:

## [P11822 [湖北省选模拟 2025] 团队分组 / divide](https://www.luogu.com.cn/problem/P11822) (Hard)

>

TODO:

# 20 套

## [P13819 「LDOI R3」玩玩数字](https://www.luogu.com.cn/problem/P13819) (Ad-hoc)

> TODO:

对于一个集合 $S$，小 D 有必胜策略当且仅当 $\sum_{i \in S} \frac 1 {2^i} \ge 1$。

显然这个条件是符合双指针的。

$$\sum_{i=l}^r \frac 1 {2^{a_i}} \ge 1$$

$$\sum_{i=l}^r \frac {2^n} {2^{a_i}} \ge 2^n$$

令 $b_i = \frac 1 {2^{a_i}} \times 2^{n-1}$，TODO:

线段树维护。TODO:

## [P11433 [COCI 2024/2025 #2] 三角 / Trokuti](https://www.luogu.com.cn/problem/P11433) (Ad-hoc)

略。

## [P11660 我终将成为你的倒影](https://www.luogu.com.cn/problem/P11660) (Normal)

分块

## [P12576 [UOI 2021] 数字图](https://www.luogu.com.cn/problem/P12576) (Normal)

